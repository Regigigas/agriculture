{
  "name": "agriculture-management-system",
  "version": "1.0.0-beta.3",
  "private": true,
  "scripts": {
    "dev:api": "npm --prefix after run start:dev",
    "dev:web": "npm --prefix before run dev:web",
    "dev:app": "npm --prefix APP run dev:h5",
    "dev:desktop": "npm --prefix before run dev",
    "build:api": "npm --prefix after run build",
    "build:web": "npm --prefix before run build:web",
    "build:app": "npm --prefix APP run build:h5",
    "build:app-resource": "npm --prefix APP run build:app",
    "build:desktop": "npm --prefix before run build",
    "pack:desktop": "npm --prefix before run pack",
    "test": "npm --prefix APP test && npm --prefix after test && npm --prefix before/server test",
    "verify:sync": "npm --prefix before run verify:sync",
    "verify:local-sync": "npm --prefix before run verify:local-sync"
  }
}
